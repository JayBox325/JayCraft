<input
    type="{{ type }}"
    id="{{ id }}"
    name="{{ name }}"
    {{ placeholder ? 'placeholder=' ~ placeholder }}
    {{ required ? '' : 'required aria-required="true"' }}
    class="field"
    value="{{ value }}"
    data-field
/>

{#

    Include with:

    {% include 'form/fields/_index' with {
        type: 'text',
        label: 'Your location',
        helptext: '',
        required: false,
        id: 'locationField',
        placeholder: 'Where are you',
        value: '',
        class: 'lg:px-2 lg:flex-1'
    } %}

#}